package com.winecraft.winecraftmod.common.block;import com.winecraft.winecraftmod.WinecraftConstants;import net.minecraft.block.Block;import net.minecraft.block.SoundType;import net.minecraft.block.material.Material;import net.minecraft.item.BlockItem;import net.minecraft.item.Item;import net.minecraft.item.ItemGroup;import net.minecraft.util.ResourceLocation;import java.util.HashMap;import java.util.Map;import static java.util.Objects.requireNonNull;public class BlockTemplates {    private static final Map<WinecraftBlock, Block> blocks = new HashMap<>();    private static final Map<WinecraftBlock, BlockItem> blockItems = new HashMap<>();    static {        //TODO: This information should be obtainable from JSON. Write a Jackson wrapper.        blocks.put(WinecraftBlock.CASK, new Block(Block.Properties.create(Material.WOOD)                .hardnessAndResistance(2.5f)                .sound(SoundType.WOOD))                .setRegistryName(WinecraftConstants.MODID, "cask"));        blockItems.put(WinecraftBlock.CASK, new BlockItem(blocks.get(WinecraftBlock.CASK), new Item.Properties()                .group(ItemGroup.DECORATIONS)));    }    public static Block getBlockFor(WinecraftBlock block) {        return blocks.get(block);    }    public static Item getItemFor(WinecraftBlock block) {        ResourceLocation registryName = requireNonNull(blocks.get(block).getRegistryName(), "registryName");        return blockItems.get(block)                .setRegistryName(registryName);    }}